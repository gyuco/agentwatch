{
  "name": "agentwatch",
  "version": "0.1.0",
  "description": "Live dashboard for Claude Code: see the main agent and all subagents working in real time, including skill usage, via non-blocking hooks",
  "type": "module",
  "bin": {
    "agentwatch": "src/cli.js"
  },
  "files": [
    "src",
    "dashboard",
    "catalog"
  ],
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/cli.js && node --check src/server.js && node --check src/collector.js && node --check src/scan.js && node --check src/settings.js && node --check src/paths.js && node --check src/run-hook.js && node --check src/ask-hook.js && node --check src/offices.js && node --check src/queue.js && node --check src/workflow.js && node --check src/catalog.js && node --check dashboard/map.js && node --check dashboard/app.js"
  },
  "keywords": [
    "claude-code",
    "hooks",
    "subagents",
    "monitoring",
    "dashboard",
    "skills"
  ],
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.232"
  }
}
